# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Artefactos de build / empaquetado
/dist
*.pdb

# Config local del usuario (rutas de su maquina, credenciales)
config.local.toml
*.local.toml

# Sets de mods de prueba / payloads grandes (no versionar binarios pesados)
/test-mods
*.pck
*.dll

# Editor / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Config del agente (Claude Code) — no se versiona
/.claude/

# Salida de publicar sets (sts2-modsync publish)
/set-publish/
